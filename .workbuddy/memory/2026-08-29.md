# 2026-08-29

## 新增：Docker 部署 Next.js + Prisma + PostgreSQL 实战指南
- 依据用户桌面部署笔记（docker常用命令.md，含 buildx 跨平台构建、Harbor 私仓 login/push/pull、run.sh 脚本、发版四连流程）整编；**笔记中的真实账号密码已替换为占位符，未写入博客页面**
- 新建 `learningDockerNextjs.html`（教程系列第 5 篇，沿用同套深色绿主题 CSS）
- 11 章：整体架构（app/db 双容器、Prisma 是依赖不是服务、localhost 大坑预告）/ 项目准备（output:'standalone'、.dockerignore、三份 env 文件、密码安全红线）/ Dockerfile 三阶段构建（deps→builder→runner、alpine openssl、standalone 手动拷 node_modules/.prisma、HOSTNAME=0.0.0.0、CMD 先 migrate deploy）/ Compose 编排（pgdata 数据卷、healthcheck+depends_on、.env 注入密码）/ 本地构建（buildx --platform linux/amd64、--load 陷阱）/ 推送镜像（私仓/Docker Hub/阿里云 ACR 选型、版本号规范）/ 服务器运行（compose 与纯 docker run 双方式、run.sh 固化命令）/ 数据库迁移（migrate dev/deploy/db push 三命令对比、psql 手动操作、pg_dump 备份）/ 发版完整流程（buildx --push 一步到位、stop+rm+run、回滚）/ 15 项报错速查表 + 排障三板斧 / 7 步验证清单 + 配置文件清单 + 十句口诀
- 同步更新 `indexCourse.html`：新增 Docker 部署 Next.js 卡（🐳 图标，环境搭建分类），教程计数 4 → 5
